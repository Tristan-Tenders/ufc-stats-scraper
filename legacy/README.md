# legacy

The original fighter-profile scrapers and the deprecated synthetic-matchup pipeline. Kept for reference; nothing here is used by pipeline.py. The old data files they produced still live in stats/ (fighter_stats.json, matchups.csv, fighters_flat.csv).
